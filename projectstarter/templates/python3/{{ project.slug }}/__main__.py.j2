{% if options.logging is defined -%}
from {{ project.slug }}.utils import logger
{%- endif %}


def main():
    """
    Entrypoint of the program.
    """
    {% if options.cli is defined -%}
    return commands.parse()
    {%- else -%}
    {% if options.logging is defined -%}
    logger.setup()
    {%- endif %}
    print("Running project {{ project.name }}")
    {%- endif %}


if __name__ == "__main__":
    exit(main())

